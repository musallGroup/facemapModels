# FrontView Playground

Retained experimental helper:

- `fM_find_tongue_frames_MD.py`: searches sessions for likely tongue events and exports candidate frames for further refinement.

Frame selection, annotation, review, model import, and model refinement belong in LabelForge. Historical FrontView training and QC scripts remain available through Git history.
